{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h1>Merci pour votre commande</h1><hr>
        <h2>{{ user.Prenom }} {{ user.nom }}</h2>
        <p>Voici votre n° de commande : #{{ commande.id }}</p>
        <p>{{ commande.dateCommande | date }}</p>
        <a href="{{ path('commande') }}">Accéder à Mes commandes</a>
    </div>
{% endblock %}
